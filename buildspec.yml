version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - chmod +x gradlew
  build:
    commands:
      - echo "Building project..."
      - ./gradlew clean build -x test

artifacts:
  files:
    - build/libs/*.jar
    - appspec.yml
    - scripts/deploy.sh
